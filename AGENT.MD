# AGENT.MD

## 커밋 메시지
- 커밋 코멘트는 가능한 한글로 작성한다.
- 외부 도구명, 라이브러리명, 고유 명사, 명령어 등은 필요한 경우 원문 표기를 유지한다.

## 버전 및 커밋 운영
- 사용자 별도 지시 없이 버전명을 업데이트하지 않는다.
- 버전 종료는 사용자가 에이전트에게 명시적으로 지시할 때만 처리한다.
- 사용자 별도 지시 없이 커밋하지 않는다.

## Plan 문서 작성 기준
- Plan 문서는 `코드 수정 기록`까지를 기획서 최상위 고정 구간으로 배치한다.
- 최상위 고정 구간에는 작업 구분, 작업 사항, 관련 이전 버전, 코드 수정 기록을 포함한다.
- 최상위 고정 구간 아래에는 구분선 `---`을 배치해 이후 기획 항목과 시인성을 분리한다.
- 기획 내용, 분석 내용, 구현 방향은 구분선 아래 별도 기획 항목으로 작성한다.
- 분석 내용이 보조 성격인 경우 문서 최하단에 `별도 기재` 항목으로 배치한다.
